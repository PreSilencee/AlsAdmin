<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alsadmin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/alslogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/alslogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlsAdmin2">
        <activity android:name=".CreateAdminActivity"></activity>
        <activity android:name=".ui.organization.OrganizationViewActivity" />
        <activity android:name=".ui.organization.OrganizationActivity" />
        <activity android:name=".ui.event.EventViewActivity" />
        <activity android:name=".ui.event.EventActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>